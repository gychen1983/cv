# Interactive Resume (initial commit)

This repository will hold an interactive, responsive web resume for Chen Guangyao (陈光耀). Work planned on branch `interactive-resume` will include:

- OCR and structured HTML of the provided resume (images 5–6)
- A responsive, animated resume page (HTML/CSS/JS) or React + Vite implementation (to be decided)
- A "Success Cases" gallery using the provided images (images 1–4) with Lightbox and optimized images (WebP/AVIF + srcset)

This README is an initial placeholder to create the repository's default branch so feature branches can be created.
